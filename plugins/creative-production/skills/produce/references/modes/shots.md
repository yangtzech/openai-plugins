# Shots

Use when an existing image or selected board item should become alternate camera and composition variants.

Explore overhead, three-quarter, side profile, wider context, tighter crop, pan, zoom, macro detail, low hero angle, and one useful surprise angle.

Use the route builder under `../../helpers/shots/` when a structured camera-variant set is useful.

Always load `../contracts/source-preservation.md`. Vary camera and composition while preserving subject identity.
